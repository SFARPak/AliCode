// Setup file for Vitest to configure environment for Ink components
// Ensure NODE_ENV is set to 'development' for Ink/React compatibility
process.env.NODE_ENV = "development"
console.log("SETUP TESTS LOADED, NODE_ENV:", process.env.NODE_ENV)
